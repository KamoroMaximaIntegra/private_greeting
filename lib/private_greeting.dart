library;

/// Returns a greeting from the private package.
String getGreeting(String organizationName) {
  // Output dari paket ini sengaja dibuat dalam Bahasa Inggris
  return "Hello from the private package for $organizationName!";
}